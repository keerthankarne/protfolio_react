# Port the Express server listens on
PORT=5001

# Origin allowed to make CORS requests (your Vite dev server)
CLIENT_ORIGIN=http://localhost:5173

# Where contact form submissions are persisted (JSON file storage)
CONTACTS_FILE=./src/data/contacts.json
